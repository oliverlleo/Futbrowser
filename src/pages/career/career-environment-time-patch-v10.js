function applyCareerEnvironmentLayout(){document.querySelectorAll('#environmentList .environment-item').forEach(item=>{const state=item.querySelector('.environment-state');const trend=item.querySelector('.environment-trend');item.style.display='grid';item.style.gridTemplateColumns='minmax(0, 1fr) 86px 18px';item.style.columnGap='8px';item.style.justifyContent='initial';if(state){state.style.minWidth='72px';state.style.textAlign='center';state.style.justifySelf='start';}if(trend){trend.style.marginLeft='0';trend.style.width='18px';trend.style.textAlign='center';trend.style.justifySelf='end';}});}document.addEventListener('career:hub-rendered',applyCareerEnvironmentLayout);applyCareerEnvironmentLayout();